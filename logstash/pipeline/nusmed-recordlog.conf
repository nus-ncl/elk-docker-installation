input {
  jdbc {
    jdbc_driver_library => "/usr/share/java/mysql-connector-java-8.0.20.jar"
    jdbc_driver_class => "com.mysql.jdbc.Driver"
    jdbc_connection_string => "jdbc:mysql://10.10.0.151:3306/nusmed_prod_log_db?serverTimezone=Asia/Singapore"
    jdbc_user => "root"
    jdbc_password => "NUSMed-03-rootP@ssw0rd"
    parameters => {}
    schedule => "*/5 * * * *"
    statement => "select * from record"
  }
}

output{
    elasticsearch { 
      hosts => ["elasticsearch:9200"]
      user => "elastic"
      password => "${ELASTIC_PASSWORD}"
      # index => "nusmed.recordlog-%{+YYYY.MM.dd}" 
      index => "nusmed.recordlog" 
    }
}
